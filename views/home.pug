extends layouts/main
include mixins/videoBlock

block content
    .videos 
        .videos
            each item in videos
                + videoBlock({
                    title : item.title,
                    views : item.views,
                    videoFile : item.videoFile
                })